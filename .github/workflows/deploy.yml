name: Build and Deploy

on:
  # Daily at 00:05 UTC = 08:05 Beijing time (matches SPEC.md)
  schedule:
    - cron: '5 0 * * *'
  # Manual trigger
  workflow_dispatch:
  # Run on push to main (when summaries are updated)
  push:
    branches:
      - main
    paths:
      - 'index.html'
      - 'css/**'
      - 'js/**'
      - 'data/summaries/**'
      - 'scripts/**'
      - '.github/workflows/**'

jobs:
  aggregate-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Required for workflow to push changes back to repo
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Install wrangler
      - name: Install Wrangler
        run: npm install -g wrangler

      # Configure Cloudflare credentials
      - name: Configure Cloudflare credentials
        run: |
          echo "CLOUDFLARE_API_TOKEN=${{ secrets.CLOUDFLARE_API_TOKEN }}" >> $GITHUB_ENV
          echo "CLOUDFLARE_ACCOUNT_ID=${{ secrets.CLOUDFLARE_ACCOUNT_ID }}" >> $GITHUB_ENV
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      # Run aggregation script
      - name: Run aggregation script
        run: node scripts/aggregate.js

      # Inject build time into index.html
      - name: Inject build time
        run: |
          BUILD_TIME=$(date -u +'%Y-%m-%d %H:%M:%S UTC')
          sed -i "s/BUILD_TIME_PLACEHOLDER/${BUILD_TIME}/g" index.html

      # Configure git
      - name: Configure git
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"

      # Commit and push changes (only if there are changes)
      - name: Commit and push changes
        run: |
          git add data/summaries/
          if git diff --staged --quiet; then
            echo "No new data to commit"
          else
            git commit -m "chore: update summary data $(date -u +'%Y-%m-%dT%H:%M:%SZ')"
            git push
            echo "Pushed updated summary data"
          fi

      # Deploy to Cloudflare Workers/Pages
      - name: Deploy to Cloudflare
        run: |
          wrangler pages deploy . --project-name=summary-dashboard --commit-message="${{ github.event.head_commit.message || github.workflow }}"
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
