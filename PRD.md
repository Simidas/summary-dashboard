# 个人经营复盘系统迭代 PRD

> 当前待开发版本以 [docs/vnext-online-recording-prd.md](docs/vnext-online-recording-prd.md) 为准。
>
> vNext 已收敛为：Google 登录、在线记录、AI 陪伴行动建议、首页提醒、轻量宠物激励与正反馈。复杂游戏化、多用户、指定人分享、Hermes 接入等全部放入后续版本。

## 1. 背景

当前站点已经具备基础的信息展示能力：每日记录、周/月/年聚合、标签、评论区和静态部署。但产品体验更接近“工作日志展示站”，还没有充分承担“总结与复盘”的核心价值。

更准确的产品定位应从“AI 工作复盘站”升级为“个人经营复盘系统”。它不只服务工作，也服务作者正在长期经营的四条人生主线：

1. **主业**：公寓租赁行业系统的后端开发。
2. **副业**：网站产品出海方向。
3. **生活和自我**：夫妻关系、父子关系、儿女关系、家庭经营、身心状态。
4. **内容产出**：从主业、副业、生活中提炼可输出的公众号内容。

现状主要问题：

- 记录了“做了什么”，但较少回答“为什么做、做得怎么样、接下来怎么改”。
- Daily 视图是内容中心，容易形成流水账阅读体验。
- Weekly/Monthly/Yearly 主要统计数量，缺少洞察、判断和闭环。
- Follow-up 只是文本列表，无法追踪状态、延期和完成情况。
- 项目维度较弱，用户难以看到一条 AI 产品线从开始到推进、卡点、决策和结果的全过程。
- 缺少低门槛的即时日记入口，很多临时想法、情绪波动和碎碎念没有被承接，也无法反哺长期复盘。
- 主业、副业、生活、内容之间存在大量联动，但当前没有统一的场景分类和复用机制。

本轮迭代目标是把站点从“展示记录”升级为“驱动行动、沉淀洞察、经营长期主线的个人复盘系统”。

试运行阶段停用 Hermes 基于对话内容自动生成 Daily JSON 的流程，改为作者手动输入。系统需要先验证“手动记录是否足够低摩擦、是否真正能驱动行动”，再决定是否重新引入自动化。

## 2. 产品目标

### 2.1 核心目标

让用户打开站点后，能在 1 分钟内回答：

- 主业、副业、生活、内容四个场景各自的当前重点是什么？
- 今天最应该推进的一件事是什么？
- 哪些问题反复卡住？
- 哪些行动项还没有闭环？
- AI 在我的工作中真正放大了什么能力？
- 最近我的思绪和情绪出现了什么模式？
- 下周应该继续什么、停止什么、减少什么？
- 哪些经历可以转化为内容选题？

### 2.2 用户价值

对站点作者：

- 把零散工作记录沉淀成可复用的复盘资产。
- 发现目标分散、执行卡点、项目推进慢等长期模式。
- 让 Follow-up 形成闭环，而不是沉没在每日记录里。
- 给随时出现的想法和情绪一个低压力出口，并通过 AI 分析获得温和反馈。
- 在主业、副业、生活、内容之间建立连接，把经历转化为行动、关系修复和内容素材。

对外部读者：

- 快速理解作者近期在做什么 AI 产品/自动化探索。
- 看到真实的过程、决策、踩坑和调整。
- 从作者对工作、副业、生活和内容的经营方式中获得启发，而不只是阅读日志。

## 3. 非目标

本轮不做：

- 复杂账号系统。
- 后台 CMS。
- 大规模数据库迁移。
- 完整 AI 自动分析服务。
- 心理诊断、治疗建议或医疗判断。
- 社交化动态流。
- 过度图表化的数据大屏。

本轮优先保持当前静态站架构，用 JSON 数据和构建期脚本完成核心体验升级。

## 4. 成功指标

### 4.1 产品指标

- 首页首屏能展示本周总结、关键成果、阻塞和下周行动。
- 首页能展示四个经营场景的当前重点、下一步和未闭环事项。
- Weekly 视图不再只是卡片列表，必须包含结构化洞察。
- 至少一个项目能被独立追踪，形成项目线视图。
- Follow-up 能区分 open / closed / dropped / deferred。
- 每条 Daily 记录能表达目标、结果、反思和下一步。
- Diary 能记录随时想法，并为每条记录生成 AI 分析与建议。
- 内容产出能从主业、副业、生活记录中提取选题线索。

### 4.2 使用指标

- 作者每周至少能产出 1 条 weekly insight。
- 80% 以上的 follow-up 有状态。
- 用户从首页进入项目线或周复盘的路径不超过 1 次点击。
- 外部读者无需展开每日卡片，也能理解当前工作主线。
- 作者能在 30 秒内完成一条 Diary 记录。
- Diary 中 80% 以上的记录拥有 AI 分析结果。
- 每周至少沉淀 3 条内容素材或选题候选。

## 5. 目标用户与场景

### 5.1 作者本人

场景：

- 每天结束时记录当天工作。
- 记录主业交付、业务理解、技术卡点和协作问题。
- 记录副业产品实验、流量、转化、收入和市场判断。
- 记录家庭关系、自我状态、陪伴质量和重要沟通。
- 从工作、副业、生活中提炼公众号选题。
- 随时记录想法、情绪、灵感、焦虑和碎碎念。
- 每周回看这一周是否围绕主线推进。
- 找出长期拖延、目标分散、AI 使用低效的问题。
- 回看一段时间内的情绪模式和反复出现的念头。
- 和家人或朋友同步最近在忙什么。

### 5.2 外部读者

场景：

- 想知道作者最近在做哪些 AI 产品。
- 想了解一个 AI 项目从想法到落地的真实路径。
- 想了解作者在主业系统开发、副业产品、家庭生活之间如何取舍和平衡。
- 想参考作者的 AI 工作流和复盘模板。

## 6. 信息架构

建议将当前四个视图升级为“Domain + 时间 + 项目 + 日记”的结构。核心不是增加很多页面，而是让每条记录都能归属到一个经营场景。

```text
Home
  - 四个经营场景总览
  - 今日最重要的一件事
  - 各场景下一步
  - 未闭环行动项
  - 内容素材候选

Domains
  - 主业
  - 副业
  - 生活和自我
  - 内容产出

Daily
  - 每日记录
  - 目标 / 结果 / 卡点 / 反思 / 下一步

Weekly
  - 四场景周总结
  - 本周成果
  - 本周卡点
  - 模式识别
  - 下周行动

Projects
  - 项目列表
  - 项目时间线
  - 项目决策
  - 项目卡点
  - 下一步

Diary
  - 随手记录
  - 情绪/主题标签
  - AI 分析
  - AI 建议
  - 私密/公开状态

Monthly
  - 月度主题
  - 项目推进对比
  - 重复问题
  - 行为模式
  - 情绪模式
  - 下月策略
```

Yearly 可暂时降级为归档入口，不作为本轮重点。

### 6.1 四个经营场景

#### 主业: Work

定位：公寓租赁行业系统的后端开发复盘。

关注点：

- 业务理解。
- 技术交付。
- 系统设计。
- 历史包袱。
- 协作沟通。
- 技术沉淀。

典型问题：

- 今天推进了哪个业务模块？
- 哪个业务规则最复杂？
- 哪个技术债需要处理？
- 哪个经验可以沉淀成文章？

#### 副业: Side Business

定位：网站产品出海方向的实验和增长复盘。

关注点：

- 产品假设。
- MVP 进展。
- 用户反馈。
- 流量来源。
- 转化率。
- 收入和成本。

典型问题：

- 当前产品假设是什么？
- 今天验证了什么？
- 哪个实验应该继续？
- 哪个实验应该停止？

#### 生活和自我: Life

定位：家庭关系、自我状态和生活质量复盘。

关注点：

- 夫妻关系。
- 亲子陪伴。
- 家庭沟通。
- 情绪状态。
- 身体和精力。
- 自我照顾。

典型问题：

- 今天有没有高质量陪伴？
- 哪个沟通可以修复？
- 我现在的情绪状态是什么？
- 什么事情正在消耗我？

#### 内容产出: Content

定位：从主业、副业和生活中提炼可公开输出的内容。

关注点：

- 公众号选题。
- 素材收集。
- 观点提炼。
- 案例沉淀。
- 草稿推进。
- 发布复盘。

典型问题：

- 今天哪些经历可以变成内容？
- 哪个问题值得写成文章？
- 哪条记录可以扩展成提纲？
- 哪篇内容需要继续推进？

### 6.2 统一记录模型

所有记录都应尽量带上 `domain` 和 `type`。这样同一条记录可以进入不同视图，而不是被锁死在 Daily 或 Diary 里。

```json
{
  "id": "record-20260415-001",
  "date": "2026-04-15",
  "domain": "work",
  "type": "blocker",
  "content": "租赁合同续约状态流转比预期复杂，老数据状态不统一。",
  "projects": ["租赁合同系统"],
  "tags": ["后端开发", "状态机", "历史数据"],
  "nextActions": ["画出现有合同状态流转图"],
  "contentSeeds": ["复杂业务系统里的状态设计经验"]
}
```

#### Domain 枚举

```text
work: 主业
side_business: 副业
life: 生活和自我
content: 内容产出
```

#### Type 枚举

```text
progress: 进展
thought: 想法
decision: 决策
blocker: 卡点
followup: 行动项
reflection: 反思
diary: 日记
content_seed: 内容素材
```

### 6.3 跨场景复用规则

一条记录可以属于一个主 domain，但可以派生到其他场景。

示例：

```text
原始记录：
今天处理租赁合同续约状态，发现老数据状态非常混乱。

主业复盘：
业务状态流转复杂，需要画状态图。

内容素材：
可写《复杂业务系统里的状态机设计》。

Follow-up：
明天梳理异常状态列表。
```

这套机制的目标是：不要求作者重复记录四遍，而是让 AI 从一条记录中提取多个用途。

## 7. 功能需求

### 7.1 Home: 四场景经营面板

#### 目标

首页从 Daily 日志切换为“四场景经营面板”，让用户一打开就知道主业、副业、生活和内容各自的当前重点，以及今天最应该推进的一件事。

#### 页面模块

- 今日最重要的一件事。
- 四个场景概览卡片：主业 / 副业 / 生活和自我 / 内容产出。
- 每个场景展示：当前重点、下一步、卡点、未闭环事项。
- 本周一句话总结。
- 本周关键成果 Top 3。
- 内容素材候选。
- AI 杠杆观察：AI 本周在哪些环节真正提效。

#### 数据来源

优先读取：

```text
data/summaries/insights/weekly/YYYY-WXX.json
data/summaries/domains/overview.json
```

如果 weekly insight 不存在，则 fallback 到现有 weekly summary。

#### 验收标准

- 首页首屏必须出现四个场景概览。
- 每个场景至少显示一个下一步或空状态。
- 首页必须突出“今天最重要的一件事”。
- 没有 weekly insight 时，页面仍可正常渲染。
- 点击场景卡片可进入对应 Domain 详情。

### 7.2 Daily: 每日复盘结构升级

#### 目标

让每日记录从“发生了什么”变成“今天如何推进目标，以及学到了什么”。每日数据统一使用新结构，旧 Hermes Daily JSON 只通过迁移脚本转成新结构，不再作为页面运行时的数据源。

#### 新增字段

```json
{
  "domain": "work",
  "type": "progress",
  "goal": "今天最重要的目标",
  "outcome": "实际完成结果",
  "blockers": ["卡点 1", "卡点 2"],
  "decisions": ["关键决定 1"],
  "nextActions": ["下一步行动 1"],
  "contentSeeds": ["可扩展成公众号的素材"],
  "reflection": "一句话复盘",
  "focusScore": 3,
  "energyScore": 4,
  "aiLeverage": {
    "usedFor": ["代码 review", "产品设计"],
    "workedWell": "AI 帮助快速定位问题",
    "didNotWork": "需求判断仍需要人工取舍"
  }
}
```

#### 展示规则

- 卡片收起态展示：日期、目标、结果、标签。
- 卡片收起态需要显示所属场景。
- 展开态展示：成就、卡点、决策、反思、下一步、AI 杠杆。
- 如果存在 `contentSeeds`，展示为“可沉淀内容”。
- 迁移后的历史数据按新结构展示；旧字段兼容仅作为安全兜底，不作为长期数据模型。

#### 验收标准

- 旧 JSON 数据仍能显示。
- 新 JSON 数据能展示完整复盘结构。
- 新 JSON 数据能按 domain 分类进入对应场景视图。
- `blockers` 和 `nextActions` 在视觉上比普通讨论更突出。

### 7.3 Weekly: 周复盘洞察

#### 目标

Weekly 不再只展示聚合数字，而是产出可阅读的周总结。

#### 新增数据文件

```text
data/summaries/insights/weekly/YYYY-WXX.json
```

#### 数据结构

```json
{
  "week": "2026-W16",
  "summary": "本周围绕 PDF Q&A 明确了主线，但执行仍被多项目牵引分散。",
  "theme": "聚焦 AI 产品建设",
  "wins": ["明确 PDF Q&A 为本月核心目标"],
  "blockers": ["目标分散", "执行节奏不稳定"],
  "patterns": ["运动恢复后专注度更好", "项目命名不统一影响回顾"],
  "decisions": ["本月主线聚焦 PDF Q&A"],
  "nextWeekFocus": ["推进 PDF Q&A MVP", "减少非主线功能改动"],
  "aiLeverage": ["用 AI 辅助梳理产品开发流程"],
  "openFollowUps": ["推进 PDF Q&A 项目 MVP"]
}
```

#### 页面模块

- 周主题。
- 一句话总结。
- 本周胜利。
- 本周阻塞。
- 模式识别。
- 关键决策。
- 下周聚焦。
- 未闭环事项。

#### 验收标准

- Weekly 顶部必须先显示 insight，再显示原有周卡片。
- insight 缺失时显示自动聚合摘要。
- 不再把统计数字作为唯一核心内容。

### 7.4 Projects: 项目线视图

#### 目标

按项目沉淀工作过程，解决当前只能按时间看、难以复盘主线的问题。

#### 数据来源

从 daily JSON 的 `projects`、`decisions`、`blockers`、`nextActions` 聚合生成：

```text
data/summaries/projects/manifest.json
data/summaries/projects/{project-slug}.json
```

#### 项目数据结构

```json
{
  "name": "PDF Q&A",
  "slug": "pdf-qa",
  "status": "active",
  "summary": "围绕 PDF 文档问答能力构建 MVP。",
  "firstSeen": "2026-04-15",
  "lastUpdated": "2026-04-15",
  "timeline": [
    {
      "date": "2026-04-15",
      "achievements": ["确定本月核心目标：AI产品建设（PDF Q&A）"],
      "decisions": ["本月主线聚焦 PDF Q&A"],
      "blockers": [],
      "nextActions": ["推进 PDF Q&A 项目 MVP"]
    }
  ],
  "openFollowUps": ["推进 PDF Q&A 项目 MVP"]
}
```

#### 页面模块

- 项目概览卡片。
- 项目状态：active / paused / shipped / dropped。
- 项目时间线。
- 关键决策列表。
- 当前卡点。
- 下一步。

#### 验收标准

- 至少支持从首页点击主线项目进入项目详情。
- 项目详情能按时间展示推进过程。
- 未闭环事项能在项目详情中显示。

### 7.5 Follow-up 闭环系统

#### 目标

让待办事项不再停留在文本列表，而是可以被追踪、延期、关闭。

#### 数据结构

```json
{
  "id": "followup-20260415-001",
  "text": "推进 PDF Q&A 项目 MVP",
  "domain": "side_business",
  "project": "PDF Q&A",
  "status": "open",
  "createdAt": "2026-04-15",
  "closedAt": null,
  "sourceDate": "2026-04-15"
}
```

#### 状态

- `open`: 待处理。
- `closed`: 已完成。
- `deferred`: 延后。
- `dropped`: 放弃。

#### 页面模块

- 首页展示最重要的 open follow-ups。
- Domain 详情展示该场景相关 follow-ups。
- Project 详情展示该项目相关 follow-ups。
- Weekly 展示本周新增和仍未关闭事项。

#### 验收标准

- open follow-up 能跨天保留。
- follow-up 能按 domain 过滤。
- closed follow-up 不再出现在首页待办区。
- 超过 7 天未关闭的事项需标记为 overdue。

### 7.6 Monthly: 月度模式复盘

#### 目标

Monthly 关注长期模式，而不是单纯成就数量。

#### 页面模块

- 本月主题。
- 最高投入项目。
- 最高产出项目。
- 反复出现的阻塞。
- 被搁置最多的 follow-up。
- AI 使用模式。
- 情绪模式和能量变化。
- 下月策略。

#### 验收标准

- 月度页必须出现“重复问题”和“下月策略”。
- 图表只作为辅助，不作为主体。

### 7.7 Diary: 随手日记与 AI 情绪分析

#### 目标

提供一个低压力、随时可写的私人出口，用来记录每天的想法、碎碎念、情绪波动、灵感、困惑和自我对话。AI 为每条记录生成温和分析和建议，帮助作者理解自己当下的状态，而不是把所有内容都包装成工作成果。

Diary 与 Daily 的区别：

- Daily 面向工作复盘，结构化记录目标、结果、卡点和行动。
- Diary 面向内在状态，允许无结构、碎片化、私人化表达。
- Daily 更适合公开展示；Diary 默认私密，只有作者主动选择才公开。

#### 典型场景

- 早上突然想到一个产品想法，先记下来。
- 工作中感到烦躁、逃避、焦虑，快速写一段。
- 晚上想把今天的情绪倒出来，不需要组织成正式复盘。
- 某个念头反复出现，希望 AI 帮忙识别背后的需求或模式。
- 一段时间后回看，想知道最近自己主要在担心什么、期待什么、回避什么。

#### 页面模块

- 快速输入框。
- 今日 Diary 列表。
- 每条记录的 AI 分析卡片。
- 情绪标签和主题标签。
- 私密/公开状态标识。
- 最近 7 天情绪模式摘要。

#### 数据结构

```json
{
  "id": "diary-20260415-001",
  "date": "2026-04-15",
  "createdAt": "2026-04-15T21:35:00+08:00",
  "domain": "life",
  "content": "今天又有点分心，PDF Q&A 明明是主线，但总想去改 dashboard。",
  "visibility": "private",
  "mood": "anxious",
  "energy": 2,
  "tags": ["分心", "PDF Q&A", "执行功能"],
  "aiAnalysis": {
    "summary": "这条记录反映出主线目标明确，但执行时容易被更轻、更熟悉的小任务吸引。",
    "emotions": ["焦虑", "逃避", "自责"],
    "needs": ["确定感", "可完成的小步骤", "减少上下文切换"],
    "patterns": ["在高不确定任务前转向低阻力优化任务"],
    "reframe": "这不一定是懒，而可能是大任务不够可启动。",
    "suggestions": [
      "把 PDF Q&A 的下一步缩小到 25 分钟内能完成的一件事。",
      "允许 dashboard 想法进入 parking lot，而不是立刻执行。",
      "明天开始前先写下 PDF Q&A 的第一步，不打开其他项目。"
    ]
  },
  "linkedProjects": ["PDF Q&A", "summary-dashboard"],
  "linkedFollowUps": [],
  "reviewed": false
}
```

#### 字段说明

- `content`: 作者原始记录，不要求完整句子。
- `domain`: 这条 Diary 主要关联的场景，可由作者选择或 AI 建议。
- `visibility`: `private` / `public` / `shared`，默认 `private`。
- `mood`: AI 或作者标记的主要情绪。
- `energy`: 1-5 分，用于记录精力水平。
- `tags`: 主题标签，可由 AI 自动建议。
- `aiAnalysis.summary`: 对内容的简短理解。
- `aiAnalysis.emotions`: 情绪识别。
- `aiAnalysis.needs`: 可能未被满足的需求。
- `aiAnalysis.patterns`: 可能的行为或思维模式。
- `aiAnalysis.reframe`: 温和重述，帮助作者换一个角度理解自己。
- `aiAnalysis.suggestions`: 可执行建议，避免空泛鸡汤。
- `reviewed`: 作者是否看过并确认该 AI 分析。

#### AI 分析原则

AI 的语气必须：

- 温和、不评判。
- 具体、不空泛。
- 给出行动建议，但不强行下结论。
- 区分“推测”和“事实”。
- 不做心理诊断。
- 不使用病理化标签。
- 不替代专业心理咨询或医疗建议。

AI 输出应避免：

- “你就是……”这类确定性判断。
- 夸张的积极鼓励。
- 把所有问题都归因于性格。
- 在作者情绪低落时给过多任务。
- 将私人记录默认公开。

#### AI 分析 Prompt 要求

每条 Diary 记录的 AI 分析应输出固定结构：

```json
{
  "summary": "我理解你现在主要在表达什么",
  "emotions": ["情绪 1", "情绪 2"],
  "needs": ["可能的需求 1"],
  "patterns": ["可能的模式 1"],
  "reframe": "换一个更温和的角度看这件事",
  "suggestions": ["一个很小的下一步", "一个边界建议", "一个自我照顾建议"]
}
```

#### 隐私与安全

- Diary 默认私密，不进入公开页面。
- 公开展示必须由作者主动设置 `visibility: public`。
- AI 分析结果默认跟随原文隐私状态。
- 短期静态站采用公开只读策略：只有 `localhost` / 本地文件环境显示 Diary 输入框，公开域名不提供写入入口。
- 前端隐藏按钮、前端密码、localStorage 标记都不算真正鉴权，只能作为体验兜底。
- 如果后续需要在公开网站上直接写 Diary，必须引入服务端鉴权，例如 Cloudflare Access + Pages Functions + D1/KV/R2。
- 可以支持“只公开 AI 总结，不公开原文”的模式，但必须明确标识。
- 如果记录中出现极端负面、自伤或危险信号，AI 只能提供支持性回应，并建议联系可信任的人或专业支持，不做进一步诊断。

#### 与复盘系统的关系

Diary 不直接替代 Daily，但可以反哺 Weekly/Monthly：

- Weekly 可汇总“本周高频情绪”和“反复出现的念头”。
- Monthly 可展示“情绪模式”和“能量变化”。
- Projects 可关联某些项目引发的情绪或阻塞。
- Follow-up 可从 Diary 中提取轻量行动项，但必须经作者确认。

#### 页面展示规则

- 默认只在作者本地或私密视图展示 Diary。
- 首页最多展示“最近情绪趋势”，不直接暴露原文。
- Diary 列表按时间倒序排列。
- AI 分析默认折叠，点击展开。
- 建议使用柔和、低压力的视觉风格，避免像任务管理器。

#### 验收标准

- 用户可以记录一条无结构 Diary。
- 每条 Diary 能显示 AI 分析和建议。
- Diary 默认 `private`。
- public Diary 才能进入公开展示。
- Weekly 能读取 Diary 的情绪/主题摘要，但不泄露 private 原文。
- AI 分析中不得出现诊断性结论。

### 7.8 Domains: 四场景经营视图

#### 目标

让主业、副业、生活和内容各自拥有独立视图，避免所有记录都堆在时间线里。Domain 视图负责回答：这个场景当前重点是什么、下一步是什么、卡在哪里、哪些事情没闭环。

#### 页面模块

- 场景标题和当前定位。
- 当前重点。
- 下一步行动。
- 未闭环 follow-ups。
- 最近 7 天相关记录。
- 高频卡点。
- 可沉淀内容素材。

#### 主业视图字段重点

- 当前业务模块。
- 技术卡点。
- 系统设计决策。
- 可沉淀技术文章素材。
- 协作/需求风险。

#### 副业视图字段重点

- 当前产品。
- 本周实验。
- 流量/转化/收入数据。
- 用户反馈。
- 下一轮实验。

#### 生活和自我视图字段重点

- 当前关系重点。
- 高质量陪伴记录。
- 情绪和能量趋势。
- 需要修复的沟通。
- 自我照顾行动。

#### 内容产出视图字段重点

- 内容素材池。
- 候选选题。
- 正在写的草稿。
- 已发布内容。
- 从哪个场景提炼而来。

#### 验收标准

- 首页四个场景卡片可点击进入对应 Domain。
- Domain 能按 `domain` 字段筛选记录。
- Domain 能展示该场景下 open follow-ups。
- Content Domain 能展示从其他三个场景提取的 `contentSeeds`。

### 7.9 Content: 内容素材池

#### 目标

把主业、副业、生活中的经历转化为公众号选题和文章素材，避免有价值的思考散落在 Daily、Diary 和项目记录中。

#### 数据结构

```json
{
  "id": "content-seed-20260415-001",
  "title": "复杂业务系统里的状态机设计",
  "sourceDomain": "work",
  "sourceRecordId": "record-20260415-001",
  "status": "idea",
  "angle": "从租赁合同续约状态混乱，讲复杂业务系统如何设计状态流转。",
  "outline": [],
  "tags": ["后端开发", "业务系统", "状态机"],
  "nextAction": "整理合同状态流转图",
  "createdAt": "2026-04-15"
}
```

#### 状态

- `idea`: 只是素材。
- `outline`: 已形成提纲。
- `drafting`: 正在写。
- `published`: 已发布。
- `dropped`: 放弃。

#### 页面模块

- 今日新增素材。
- 按来源场景分组。
- 可立即写的选题。
- 正在推进的草稿。
- 已发布内容复盘。

#### AI 辅助

AI 可以从记录中提取：

- 可能的选题标题。
- 文章角度。
- 目标读者。
- 提纲草稿。
- 需要补充的案例或数据。

#### 验收标准

- 主业、副业、生活记录都可以产生 content seed。
- Content 视图能按状态筛选素材。
- 每条 content seed 能追溯到来源记录。
- 已发布内容可以关联回原始经历。

## 8. 数据生成策略

### 8.1 短期方案

保持静态 JSON 文件。

- 每日记录由人工或现有流程生成。
- Diary 记录由作者随时输入，默认存为 private JSON。
- 每条记录尽量标记 `domain` 和 `type`。
- 聚合脚本负责生成 weekly / monthly / yearly / projects / domains / content seeds。
- weekly insight 可以先人工维护，再逐步引入 AI 辅助生成。

### 8.2 中期方案

新增脚本：

```text
scripts/generate-insights.js
scripts/generate-projects.js
scripts/generate-followups.js
scripts/generate-diary-insights.js
scripts/generate-domains.js
scripts/generate-content-seeds.js
```

职责：

- 从 daily JSON 提取项目、决策、卡点和行动项。
- 生成 project timeline。
- 汇总 open follow-ups。
- 为 weekly insight 提供自动草稿。
- 为 Diary 记录生成 AI 分析结果。
- 汇总 Diary 的情绪、主题和反复出现的模式。
- 生成四个 Domain 的概览数据。
- 从主业、副业、生活记录中提取内容素材。

### 8.3 长期方案

引入 AI 辅助：

- 输入最近 7 天 daily JSON。
- 输出 weekly insight 草稿。
- 输入 Diary 原文，输出单条分析和建议。
- 输入最近 7 天 Diary 分析，输出情绪模式摘要。
- 输入所有记录，识别可能的内容选题。
- 输入四个 Domain 的记录，生成场景级总结。
- 作者人工确认后提交。
- 不自动发布未经确认的复盘判断。
- 不自动公开 private Diary 内容。

## 9. 迭代计划

### Phase 1: 四场景数据结构升级

目标：统一 Daily 数据结构，让 Daily 和 Weekly 具备复盘语义，同时支持主业、副业、生活、内容四个场景。

范围：

- 新增手动输入的每日综合记录：`data/records/daily/YYYY-MM-DD.json`。
- 将旧 Hermes Daily JSON 一次性迁移为新结构。
- 旧 Hermes 原始文件归档到 `data/legacy/hermes-daily/`，不参与页面读取和聚合。
- 新增 `domain` 和 `type` 字段。
- Daily 卡片支持新字段展示。
- 新增 weekly insight JSON。
- Weekly 顶部展示 insight。
- 首页仍保留当前 Daily，但增加“四场景概览”模块。

验收：

- 迁移后的历史 daily 数据能按新结构渲染。
- 新记录能按 domain 归类。
- 页面和聚合脚本只读取 `data/records/daily/`。
- Weekly 能显示主题、总结、卡点和下周行动。

### Phase 2: 首页改版

目标：首页从日志列表变为四场景经营面板。

范围：

- 新增 Home 视图或将 Daily 默认入口改为 Home。
- 首页展示主业、副业、生活、内容四个场景。
- 每个场景展示当前重点、下一步、open follow-ups。
- Daily 作为独立导航保留。

验收：

- 打开首页即可看到四个场景的当前状态。
- 首页到项目线不超过 1 次点击。

### Phase 3: 项目线视图

目标：按项目复盘长期推进过程。

范围：

- 生成 project JSON。
- 新增 Projects 导航。
- 新增 Project Detail。
- 项目详情显示 timeline、decisions、blockers、nextActions。

验收：

- 至少 1 个 active 项目完整展示。
- 项目视图能解释项目从何而来、现在卡在哪里、下一步做什么。

### Phase 4: Follow-up 闭环

目标：让行动项可追踪。

范围：

- follow-up 对象化。
- follow-up 支持 domain 字段。
- 支持状态、创建时间、关闭时间。
- 首页展示 overdue/open follow-ups。
- Weekly 展示新增/关闭/遗留事项。

验收：

- open follow-ups 可跨周追踪。
- follow-ups 可按四个场景过滤。
- overdue 事项有明显提示。

### Phase 5: 月度策略复盘

目标：从月度数据中看长期行为模式。

范围：

- Monthly 顶部增加模式分析。
- 展示四个场景的投入分布、重复阻塞、情绪模式、下月策略。
- 减弱纯数量图表权重。

验收：

- Monthly 能回答“这个月四个场景分别卡在哪里”和“下月应该怎么调整”。

### Phase 6: Diary 随手日记

目标：为碎片想法和情绪提供低压力出口，并让 AI 提供温和反馈。

范围：

- 新增 Diary 数据模型。
- 新增 Diary 视图。
- 支持 private / public / shared 可见性。
- 支持每条记录的 AI 分析和建议。
- Weekly/Monthly 可读取 Diary 的聚合摘要。

验收：

- 作者能快速新增一条 Diary 记录。
- 每条 Diary 可以展示 AI 分析。
- Diary 默认私密。
- private 原文不会出现在公开首页或公开周报里。
- Weekly 至少能展示“本周高频情绪/主题”的摘要。

### Phase 7: 内容素材池

目标：从主业、副业和生活记录中提取公众号内容素材。

范围：

- 新增 content seed 数据结构。
- 新增 Content Domain 视图。
- 支持素材状态：idea / outline / drafting / published / dropped。
- 支持从记录中追溯素材来源。
- AI 辅助生成标题、角度和提纲草稿。

验收：

- 主业、副业、生活记录都能产生 content seed。
- Content 视图能显示可写选题和正在推进的草稿。
- 每条已发布内容能关联回来源记录。

## 10. 优先级

### P0

- Daily 新字段兼容展示。
- Weekly insight 数据结构和页面展示。
- `domain` / `type` 统一记录模型。
- 首页增加四场景概览。

### P1

- Home 视图改版。
- Domain 视图。
- Project 视图。
- Follow-up 状态化。
- Diary 数据模型和私密记录视图。
- Content seed 数据结构。

### P2

- Monthly 模式分析。
- 搜索和标签过滤。
- AI 自动生成 weekly insight 草稿。
- Diary AI 分析和情绪模式聚合。
- AI 自动提取内容选题和提纲。

## 11. 需求池（待讨论）

### 激励系统：宠物养成

状态：需求池，暂不进入当前版本范围，下一版本迭代前再评估优先级。

初步想法：

- 通过“宠物养成”增强记录和复盘的正反馈。
- 用户完成每日记录、关闭 follow-up、完成周复盘、连续维护某个场景时，宠物获得成长反馈。
- 宠物不直接用来惩罚断档，避免把复盘系统变成压力源。
- 宠物成长应服务于“驱动记录”和“看见经营进展”，不能喧宾夺主。

待讨论问题：

- 宠物成长和哪些行为绑定最合理。
- 是否按四个场景分别影响宠物状态或形态。
- 断档时如何温和处理，不制造负罪感。
- 是否先做轻量成就/徽章，再做完整宠物养成。

### Diary 写入鉴权

状态：需求池，暂不进入当前版本范围。当前版本采用“公开站只读、本地作者环境可写”的过渡方案，后续需要在线写入时再评估优先级和技术方案。

初步想法：

- 公开站点不允许访客写入 Diary。
- 作者如果要在公开域名直接记录，需要登录后才能看到写入入口。
- private Diary 不应以静态 JSON 形式部署到公开站。
- 写入能力需要服务端鉴权，不能只靠前端隐藏按钮、前端密码或 localStorage。

可选技术方向：

- Cloudflare Access 限定作者邮箱。
- Cloudflare Pages Functions 校验身份并处理写入请求。
- D1 / KV / R2 存储 Diary、草稿或公开摘要。
- GitHub OAuth 或其他登录体系，作为后续备选。

待讨论问题：

- 是否真的需要在公开域名直接写 Diary，还是继续本地输入后提交 JSON。
- 写入后数据存到哪里：D1、KV、R2 还是 GitHub。
- private / shared / public 三种可见性如何和鉴权绑定。
- AI 分析是在写入时生成，还是批处理生成后人工确认。

### D1 数据备份与导出

状态：需求池，暂不进入当前版本范围。后续在稳定使用一段时间后评估优先级，目标是降低个人长期记录资产丢失风险。

初步想法：

- D1 继续作为在线主库，日常记录、项目、follow-up、AI 建议等都以 D1 为准。
- 提供手动导出能力，把个人数据导出为 JSON / SQL / Markdown 等可迁移格式。
- 提供定期备份能力，例如每天或每周自动导出到 Cloudflare R2、GitHub 私有仓库或其他安全位置。
- 备份应覆盖核心业务数据，不只覆盖 Diary，也包括 records、projects、followups、content_items、daily_reviews、period_reviews、user_state、ai_suggestions 等表。
- 备份方案优先保证可恢复、可迁移、可读，避免把个人数据锁死在单一平台。

待讨论问题：

- 备份目标放在哪里最合适：R2、GitHub 私有仓库、本地下载，还是多目标同时保留。
- 备份频率采用每日、每周，还是提供手动触发。
- 导出格式优先支持 JSON、SQL，还是同时生成 Markdown 方便人读。
- 是否需要提供一键恢复脚本，以及恢复时如何避免覆盖线上新数据。
- 备份文件是否需要加密，以及密钥由谁保管。

## 12. 风险与应对

### 风险 1: 数据录入负担变重

应对：

- 新字段全部可选。
- 页面兼容旧数据。
- 每日记录模板控制在 5 分钟内可完成。

### 风险 2: 复盘变成过度自我分析

应对：

- 首页只展示少量高价值结论。
- 周/月复盘强调行动，不做情绪堆叠。

### 风险 3: AI 自动总结不准确

应对：

- AI 只生成草稿。
- 所有 insight 发布前人工确认。
- 保留原始 daily 记录作为可追溯依据。

### 风险 4: 静态 JSON 结构越来越复杂

应对：

- 先用脚本聚合。
- 数据结构稳定后再考虑 Workers / D1。
- 不提前引入重型后端。

### 风险 5: Diary 内容过于私人

应对：

- Diary 默认 private。
- UI 明确标识可见性。
- public 需要显式设置。
- 周/月聚合只引用情绪和主题摘要，不直接引用 private 原文。

### 风险 6: AI 分析造成压力或误导

应对：

- AI 输出必须温和、非诊断。
- 建议必须小而具体。
- 支持作者隐藏、删除或重生成 AI 分析。
- 极端负面内容只给支持性回应和求助建议。

## 13. 推荐第一版交付范围

建议下一次实际开发只做以下内容：

1. 新增 `domain` / `type` 统一记录字段。
2. Daily 支持 `goal/outcome/blockers/decisions/nextActions/reflection/aiLeverage/contentSeeds`。
3. 首页增加“四场景概览”：主业、副业、生活和自我、内容产出。
4. 新增 weekly insight 数据结构，并按四个场景输出周复盘。
5. 预留 Diary 数据目录和字段规范，但不在第一版公开展示。
6. 预留 content seed 数据结构，先支持从记录中手动标记内容素材。
7. README 增加新版数据格式说明。

这一版完成后，站点会立刻从“工作日志展示”升级为“个人经营复盘系统”的雏形，同时不会一次性改动太大。
